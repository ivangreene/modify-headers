import {
  DELETE_SITE,
  DELETE_HEADER,
  ADD_RULE,
  DELETE_RULE,
  EDIT_RULE,
} from '../constants/ActionTypes';

const initialState = [];

export default function rules (state = initialState, action) {
  return state;
}
